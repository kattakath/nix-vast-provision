## What & why

## Checklist
- [ ] `nix flake check -L` passes (shellcheck for every CLI + scripts-lint)
- [ ] `.nix` formatted (`nixfmt-rfc-style`)
- [ ] No secret values added; templates stay secret-free (account-level vars only)
